# 🖥️ 用户界面测试指南

## 当前应用状态
✅ **应用已启动** - Electron 桌面应用正在运行  
✅ **所有核心功能测试通过** - 后端逻辑完全正常  
✅ **测试数据已准备** - 可用于界面测试  

## 📋 界面测试清单

### 1. 应用窗口检查
- [ ] 应用窗口正常显示
- [ ] 标题显示为"书签同步工具"
- [ ] 界面布局美观，无错位
- [ ] 所有按钮和输入框可见

### 2. 浏览器配置区域测试

#### Chrome 路径测试
- [ ] **点击"自动检测"按钮**
  - 预期结果: Chrome 路径自动填充为 `/Users/Winnie.C/Library/Application Support/Google/Chrome/Default/Bookmarks`
  - 状态: 路径输入框显示检测到的路径

#### Atlas 路径测试
- [ ] **点击"浏览"按钮**
  - 选择文件: `/Users/Winnie.C/Code/bookmark-sync-tool/test-atlas-bookmarks.json`
  - 预期结果: Atlas 路径输入框显示选择的文件路径

### 3. 同步设置测试

#### 同步方向选择
- [ ] **双向同步** (默认选中)
  - 点击确认选中状态
- [ ] **Chrome → Atlas**
  - 点击测试单选按钮切换
- [ ] **Atlas → Chrome**
  - 点击测试单选按钮切换
- [ ] 切换回**双向同步**

### 4. 同步功能测试

#### 手动同步测试
- [ ] **点击"手动同步一次"按钮**
  - 预期结果: 
    - 同步日志显示"开始手动同步..."
    - 显示同步完成信息
    - 更新"最后同步时间"
    - 显示同步结果 (Chrome/Atlas 更新状态)

#### 自动同步测试
- [ ] **点击"启动自动同步"按钮**
  - 预期结果:
    - 同步状态变为"运行中" (绿色)
    - "启动自动同步"按钮变为禁用
    - "停止同步"按钮变为可用
    - 日志显示"自动同步已启动"

- [ ] **点击"停止同步"按钮**
  - 预期结果:
    - 同步状态变为"已停止" (灰色)
    - "启动自动同步"按钮变为可用
    - "停止同步"按钮变为禁用
    - 日志显示"自动同步已停止"

### 5. 状态显示测试

#### 同步状态指示器
- [ ] 状态徽章颜色正确显示
  - 🟢 运行中 (绿色)
  - ⚫ 已停止 (灰色)
  - 🔴 出错 (红色，如果有错误)

#### 同步日志
- [ ] 日志区域正常显示
- [ ] 日志条目包含时间戳
- [ ] 不同类型日志颜色区分:
  - 成功: 绿色
  - 错误: 红色  
  - 信息: 蓝色
- [ ] **点击"清空日志"按钮**
  - 预期结果: 日志区域清空，显示"日志已清空"

### 6. 错误处理测试

#### 路径错误测试
- [ ] 清空 Atlas 路径，点击"手动同步"
  - 预期结果: 显示错误信息"请先设置 ChatGPT Atlas 书签路径"

#### 文件不存在测试
- [ ] 输入不存在的文件路径，点击"手动同步"
  - 预期结果: 显示文件读取错误信息

### 7. 界面响应性测试

#### 窗口调整
- [ ] 调整窗口大小，检查布局适应性
- [ ] 最小化/最大化窗口测试
- [ ] 检查移动端样式 (如果窗口很小)

#### 交互反馈
- [ ] 按钮悬停效果
- [ ] 按钮点击反馈
- [ ] 输入框焦点样式
- [ ] 加载状态显示

## 🎯 测试数据信息

### 当前 Chrome 书签
- **路径**: `/Users/Winnie.C/Library/Application Support/Google/Chrome/Default/Bookmarks`
- **书签栏数量**: 60 个
- **其他书签数量**: 1 个

### 测试 Atlas 书签
- **路径**: `/Users/Winnie.C/Code/bookmark-sync-tool/test-atlas-bookmarks.json`
- **书签栏数量**: 3 个 (ChatGPT, OpenAI Platform, AI 工具文件夹)
- **其他书签数量**: 2 个 (开发工具文件夹, 学习资源)

## 🔍 预期同步结果

### 双向同步后
- Chrome 和 Atlas 都会包含合并后的所有书签
- 不会有重复的书签 (相同 URL 会去重)
- 文件夹结构会被保留

### Chrome → Atlas 同步后
- Atlas 会完全替换为 Chrome 的书签
- Atlas 原有的独特书签会被覆盖

### Atlas → Chrome 同步后
- Chrome 会完全替换为 Atlas 的书签
- Chrome 原有的书签会被覆盖

## ⚠️ 注意事项

1. **备份重要**: 应用会自动创建 `.backup.*` 文件
2. **实时监控**: 自动同步会监控文件变化
3. **权限检查**: 确保应用有读写书签文件的权限
4. **浏览器关闭**: 建议在同步时关闭相关浏览器

## 🎉 测试完成标准

当以下所有项目都通过时，应用就可以发布使用了:

- [ ] 所有界面元素正常显示和工作
- [ ] 路径检测功能正常
- [ ] 手动同步功能正常
- [ ] 自动同步功能正常
- [ ] 错误处理友好
- [ ] 日志显示清晰
- [ ] 状态指示准确

---

**开始测试**: 现在你可以在桌面应用中按照这个清单逐项测试！